[package]
name = "sokoban-rs"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Pure Sokoban logic crate (grid + rules + serde)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
once_cell = "1"
